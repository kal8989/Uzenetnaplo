name: APK build

on:
  push:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: ZIP kicsomagolása
        run: for f in *.zip; do unzip -o "$f"; done

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

    

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: APK fordítása
        working-directory: uzenetnaplo
        run: gradle :app:assembleDebug --no-daemon

      - name: APK átnevezése
        run: cp uzenetnaplo/app/build/outputs/apk/debug/app-debug.apk uzenetnaplo.apk

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: Üzenetnapló build ${{ github.run_number }}
          files: uzenetnaplo.apk
